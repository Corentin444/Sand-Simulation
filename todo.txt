une classe simulation avec un double vector de material
taille de la simulation

classe parente material
avec static

une fonction get color

une classe air et une classe sand

return sf::Color(0, 0, 0);